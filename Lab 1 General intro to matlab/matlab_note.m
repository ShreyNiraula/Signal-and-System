% MATLAB REVSION....
% stores in the form of matrix
% +,-,*,/ perform the matrix calculation
% for corresponding value multiplication, .* to be done...

% a= [1,2,3] or a= [1 2 3] = 1x3 matrix or vector
% b= [1;2;3] = 3x1 matrix 
% c= [1 2; 3 4] = 2x2 matrix

% ; supresses the output (output not shown..)
% at default case, result is stored in ans variable

% clc to clear command
% closeAll to close all figures

% syntax of function..
    % function [op para1, op para2,...] = Name_function (ip para1, ...)
% end to terminate

% plot related
    % hold on : show all curves in same graph screen
    % hold off : reverse of hold on
    % subplot (r,c,x): divides single window to r rows, c columns and and
        % plots at xth part
    
    
